
# 📦 CLI-приложение для анализа зависимостей Maven-пакетов  
Приложение выполняет загрузку и анализ прямых зависимостей Maven-артефактов, используя POM-файлы с удалённого репозитория.  
Работа основана на JSON-конфигурации, задающей имя пакета, репозиторий и параметры фильтрации.

## 🚀 Возможности
- Загрузка параметров из `config.json`
- Проверка корректности всех конфигурационных полей
- Формирование URL к POM-файлу по Maven-структуре
- Загрузка POM через HTTP
- Парсинг XML-структуры файла
- Извлечение **прямых зависимостей**
- Фильтрация зависимостей по подстроке  
- Отображение зависимостей в консоли

## ⚙️ Формат config.json

```json
{
    "package_name": "org.apache.commons:commons-lang3",
    "repo_url": "https://repo1.maven.org/maven2/",
    "test_mode": false,
    "version": "3.12.0",
    "output_image": "graph.png",
    "max_depth": 3,
    "filter_substring": "commons"
}
```

### Описание полей

| Поле               | Тип    | Описание                                  |
| ------------------ | ------ | ----------------------------------------- |
| `package_name`     | string | Имя пакета в формате `groupId:artifactId` |
| `repo_url`         | string | URL Maven-репозитория                     |
| `test_mode`        | bool   | Флаг тестового режима                     |
| `version`          | string | Версия пакета                             |
| `output_image`     | string | Имя выходного файла (для будущих этапов)  |
| `max_depth`        | int    | Глубина анализа (для будущих этапов)      |
| `filter_substring` | string | Фильтр зависимостей                       |

## 📄 Пример вывода

```
Загружается POM-файл: https://repo1.maven.org/maven2/org/apache/commons/commons-lang3/3.12.0/commons-lang3-3.12.0.pom

=== Зависимости пакета ===
org.junit:junit-bom:5.7.1 (scope=import)
org.junit.jupiter:junit-jupiter:(не указана) (scope=test)
org.hamcrest:hamcrest:2.2 (scope=test)
org.openjdk.jmh:jmh-core:${jmh.version} (scope=test)
...
=================================
```
